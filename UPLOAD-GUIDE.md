# GitHub 처음 업로드하는 방법

저장소 이름은 **`crack-userscripts`** 로 만들면 됩니다. 저장소(repository)는 GitHub 안의 프로젝트 폴더라고 생각하면 됩니다.

1. GitHub에서 `+` → **New repository**를 누릅니다.
2. Repository name에 **`crack-userscripts`** 를 입력합니다.
3. 공개 배포할 거라면 **Public**을 선택합니다.
4. README 자동 생성 옵션은 켜지 않아도 됩니다. 이 묶음에 README가 이미 들어 있습니다.
5. **Create repository**를 누릅니다.
6. 새 저장소에서 **Add file → Upload files**를 누릅니다.
7. 이 ZIP을 풀어서 나온 파일 5개를 전부 올립니다.
8. 아래쪽 **Commit changes**를 누릅니다.
9. 업로드가 끝나면 README의 **설치하기** 링크가 동작합니다.

### 이후 업데이트

코드를 고친 뒤 같은 `.user.js` 파일을 다시 업로드하고 `@version` 숫자를 올리면 됩니다. 예: `4.3.0.10` → `4.3.0.11`.
